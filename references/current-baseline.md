# 当前基线

## 1. 最新 bundle 内全链路结果

截至 `2026-04-08`，当前 `mars-moon` bundle 已在目录内完成一次新的完整闭环验证：

- 链路：`prepare -> build -> run --dry-run -> burn -> run`
- workspace：`work/fullchain-20260408-wakephrase-v3`
- 结果目录：`work/fullchain-20260408-wakephrase-v3/result/0408103826`
- 用例总数：`73`
- 结果汇总：`PASS 64 / FAIL 9 / BLOCKED 0`

如果迁移时不保留 `work/` 目录，本页仍可作为方法基线；具体证据目录则以新机器重新执行后的结果为准。

## 2. 本轮已确认生效的关键修正

- `TC_PHRASE_003`
  - 结果导出已展开为可读的详细执行步骤。
  - 用例前置已显式加入对应窗帘模式设置。
- `TC_PHRASE_004`
  - 结果导出已展开为可读的详细执行步骤。
  - 用例前置已显式加入对应窗帘模式设置。
- `TC_PHRASE_005`
  - 结果导出已展开为逐个候选唤醒词的可读执行步骤。
- `TC_WAKEWORD_004 ~ TC_WAKEWORD_010`
  - 已按“切换后四项校验”逻辑执行通过：
    1. 新设置的唤醒词可以正常唤醒。
    2. 默认唤醒词仍可正常唤醒。
    3. 除默认词和当前设置词外，其他候选词不能唤醒。
    4. 切换完成后命令词仍能正常生效。

## 3. 当前剩余失败分类

本轮剩余 `9` 条 FAIL 已定位为需求/现网差异或固件行为问题，不属于工具链无法跑通：

- 固件/行为问题：
  - `TC_BOOT_001`
- 需求或协议期望差异：
  - `TC_UART_001`
  - `TC_UART_002`
  - `TC_CTRL_001`
  - `TC_CTRL_002`
  - `TC_CTRL_003`
  - `TC_CTRL_004`
  - `TC_CTRL_005`
  - `TC_CTRL_006`

## 4. 当前 bundle 应保持的测试口径

- 设置类命令前必须先唤醒设备。
- 每次重启后必须恢复日志等级并确认日志恢复。
- 主动重启、主动上下电不计入异常重启阈值。
- 连续 3 条用例因唤醒失败则停止执行。
- 唤醒词切换类用例必须做完整遍历校验，而不是只验证“设置成功”播报。
- 短语类验证要把模式前置、同义词集合和可见执行步骤一起落到结果里。

## 5. 使用这份基线的方法

- 想验证工具链是否仍可运行：优先重新执行 `prepare + build + run --dry-run`。
- 想验证实机链路是否仍一致：在新机器上按 `references/workflow.md` 再走一遍 `prepare -> build -> burn -> run`。
- 如果新需求引入新的状态机规则，先更新 `references/mind.md` 中的方法，再改 `tools/` 里的生成逻辑。
